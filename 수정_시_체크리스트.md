# 🔧 Card-Simulator 수정 시 체크리스트

> 시뮬레이터 코드를 수정하거나 새 카드/팩을 추가할 때 **반드시** 확인해야 할 항목들입니다.

---

## 1. 기획서 ↔ cards.js 데이터 동기화

카드 수치를 변경할 때, **기획서(마크다운)와 cards.js 양쪽을 동시에 수정**해야 합니다.


---

## 2. 새 카드 추가 시 필수 절차

1. `cards.js`에 `registerCards([...])` 배열에 카드 객체 추가
2. 새 키워드가 있으면 `effects.js`의 `registerCoreEffects()` 안에 `registry.register()` 추가
3. 필요 시 `engine.js`의 `playCard()`에 처리 로직 추가
4. `ARCHITECTURE.md` 문서도 함께 동기화

---

## 3. 밸런싱 체크리스트 (§7 축약)

- [ ] Common/Rare에서 과잉 드로우 + 자유 해체 조합이 무한 턴을 만들지 않는가?
- [ ] 네트워크 n 계수 × 0코스트 카드 다량 투입 시 수치 인플레이션 없는가?
- [ ] 프로토콜 우회(서브루틴 가동, 대역폭 증폭)로 아포칼립스 즉사가 발동되지 않는가?
- [ ] 재구축 횟수 증가 효과가 무한 루프를 만들지 않는가?
- [ ] 0코스트 + 에너지 획득 + 드로우 조합이 무한 행동이 되지 않는가?

---

## 4. 절대 금지 규칙

- ❌ **외생 변수 카드 생성(Discover/Create) 금지** — 덱에 없는 카드를 시스템이 만들어주는 기믹 불가
- ❌ **소멸 무덤(Void Pile) 참조 금지** — 무덤 회수, 소멸 장수 카운트 기믹 불가
- ❌ **파워 카드 중복 편성 불가** — 종류별 덱에 1장만

---

## 5. 모듈 의존도 (수정 영향 범위)

```
app.js  →  simulator.js  →  engine.js  →  effects.js
                          →  ai.js      →  cards.js
```

- `cards.js`만 건드리면 UI 자동 반영
- `effects.js` 수정 시 `engine.js`의 `playCard()` 연동 확인 필요
- `engine.js` 수정 시 워크플로우 §1~§4 규칙과 일치하는지 반드시 확인

---

## 6. 실행 및 테스트

```bash
npx -y http-server . -p 8080 --cors -c-1
# → http://127.0.0.1:8080
```

밸류 티어 기준: **S**(≥150%) / **A**(≥120%) / **B**(≥90%) / **C**(≥65%) / **D**(≥45%) / **F**(<45%)
